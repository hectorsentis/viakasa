Property attribute stat — pair with Lucide icons (`bed`, `bath`, `move`/`maximize`). Use in a flex row inside PropertyCard or on a listing detail.

```jsx
<FeatureStat icon={<i data-lucide="bed-double" />} value="3" label="Habitaciones" />
<FeatureStat icon={<i data-lucide="bath" />} value="2" label="Baños" />
<FeatureStat icon={<i data-lucide="maximize-2" />} value="95 m²" label="Superficie" />
```
